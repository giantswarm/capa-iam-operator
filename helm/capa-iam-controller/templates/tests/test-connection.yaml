apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "capa-iam-controller.fullname" . }}-test-connection"
  labels:
    {{- include "capa-iam-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "capa-iam-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
